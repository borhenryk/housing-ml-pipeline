name: Housing ML Pipeline - CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod

jobs:
  validate:
    name: Validate Bundle
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install Databricks CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
      
      - name: Validate Bundle
        working-directory: housing_ml_project
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "Validating Databricks Asset Bundle..."
          /usr/local/bin/databricks bundle validate -t dev
          echo "Bundle validation successful!"

  lint:
    name: Lint Python Code
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: pip install ruff black
      
      - name: Run Ruff linter
        working-directory: housing_ml_project
        run: |
          echo "Running Ruff linter..."
          ruff check src/ --ignore E501,F401 || true
      
      - name: Check formatting with Black
        working-directory: housing_ml_project
        run: |
          echo "Checking code formatting..."
          black --check src/ --line-length 100 || true

  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    needs: [validate, lint]
    environment: development
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Install Databricks CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
      
      - name: Deploy Bundle to Dev
        working-directory: housing_ml_project
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "Deploying to Dev environment..."
          /usr/local/bin/databricks bundle deploy -t dev
          echo "Deployment to Dev successful!"
      
      - name: Run Training Job
        working-directory: housing_ml_project
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "Starting training pipeline..."
          /usr/local/bin/databricks bundle run housing_ml_training_job -t dev
          echo "Training job started!"
